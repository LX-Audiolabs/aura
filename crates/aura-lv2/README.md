# aura-lv2

LV2 wrapper for [AURA](https://github.com/LX-Audiolabs/aura) (Linux ship path).

**Reduced subset:** first `bus_layouts()` entry only; no full latency/tail/
presets/transport parity with CLAP. See crate docs.

Enable via `aura = { features = ["lv2"] }`.

License: GPL-3.0-or-later.
